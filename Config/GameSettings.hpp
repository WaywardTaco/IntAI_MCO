
#pragma once
#include <string>

const std::string
    GAME_NAME = "Space Conquest";

const unsigned int
    WINDOW_WIDTH = 1600,
    WINDOW_HEIGHT = 880;

const float
    FRAME_LIMIT = 60.f,
    FRAME_TIME = 1.f / 60.f;